NODE count_logs
SQL >

    SELECT 
      company_id, 
      count() as events 
    FROM kafka_audit_log
    GROUP BY company_id



NODE endpoint
SQL >

    SELECT
      name,
      events,
      plan
    FROM count_logs
    JOIN companies
    USING company_id


