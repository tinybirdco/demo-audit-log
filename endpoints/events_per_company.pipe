NODE count_logs
SQL >

    SELECT
        company_id,
        count() AS events
    FROM kafka_audit_log
    GROUP BY company_id



NODE endpoint
SQL >

    SELECT
        name,
        events,
        plan
    FROM count_logs
    INNER JOIN companies USING (company_id)


