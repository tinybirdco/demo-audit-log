# Data Source created from Pipe 'mat_enriched_events'

SCHEMA >
    `datetime` DateTime64(3),
    `company_id` Int16,
    `name` String,
    `event` String,
    `payload_author` String,
    `payload_entity_id` String

ENGINE "MergeTree"
ENGINE_PARTITION_KEY "toYYYYMM(datetime)"
ENGINE_SORTING_KEY "company_id, datetime, event"
