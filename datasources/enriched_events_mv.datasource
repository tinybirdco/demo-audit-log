# Data Source created from Pipe 'ls_log_message_join'

SCHEMA >
    `datetime` DateTime64(3),
    `name` LowCardinality(String),
    `event` String,
    `payload_author` String,
    `payload_entity_id` String

ENGINE "MergeTree"
ENGINE_PARTITION_KEY "toYYYYMM(datetime)"
ENGINE_SORTING_KEY "event, name, datetime"
