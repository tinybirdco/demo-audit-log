NODE kafka_audit_log_minute_mat
SQL >

    SELECT 
        toStartOfMinute(datetime) t, 
        count() c
    FROM kafka_audit_log
    GROUP BY t

TYPE materialized
DATASOURCE kafka_audit_log_minute_mv
ENGINE "AggregatingMergeTree"
ENGINE_PARTITION_KEY "toYYYYMM(t)"
ENGINE_SORTING_KEY "t"

